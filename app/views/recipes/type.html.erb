<h1 class="text-center mb-4">Find Recipes by mealtime</h1>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= form_with url: recipe_type_path, method: :get, local: true do |f| %>
        <div class="mb-3">
          <label for="type" class="form-label">Enter Meal type (comma separated):</label>
          <%= f.text_field :type, placeholder: "e.g., lunch , dinner", class: "form-control" %>
        </div>
        <%= f.submit "Search", class: "btn btn-primary w-100" %>
      <% end %>
    </div>
  </div>
</div>

<% if @recipes.present? %>
  <h2 class="text-center my-4">Recipes Found</h2>
  <div class="container">
    <div class="row">
      <% @recipes.each do |recipe| %>
        <div class="col-md-4">
          <div class="card mb-4">
            <img src="<%= recipe['image'] %>" class="card-img-top" alt="<%= recipe['title'] %>" />
            <div class="card-body">
              <h5 class="card-title"><%= recipe['title'] %></h5>
              <p class="card-text"><%= recipe['instructions'] %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
<% else %>
  <p class="text-center">No recipes found.</p>
<% end %>
